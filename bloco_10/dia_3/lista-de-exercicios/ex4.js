const toUpCase = (str) => str.toUpperCase();
const firstLetter = (str) => str.split('')[0];
const concactStr = (str1, str2) => str1.concat(str2);

module.exports = { toUpCase, firstLetter, concactStr };
